import MainTemplate from '@/templates/MainTemplate';

export default function FeatureSectionsPage() {
  return (
    <MainTemplate>
      Feature Sections With product screenshot Centered 2x2
      grid With large screenshot on dark With large
      screenshot Simple three column with small icons on
      dark Simple three column with small icons With product
      screenshot on left With product screenshot on dark
      Simple three column with large icons on dark Simple
      three column with large icons Contained in panel With
      product screenshot panel With testimonial Offset 2x2
      grid With code example panel Offset with feature list
      Simple Simple two-column with small icons on dark
    </MainTemplate>
  );
}
